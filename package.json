{
  "name": "maltify",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "maltify": "node packages/cli/dist/index.js",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "dev": "pnpm --parallel -r dev"
  },
  "packageManager": "pnpm@10.34.5",
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild"
    ]
  }
}
